#include "user_funcs.h"

// when handler receives a character from the ISR,
//   if user task has stream opened for reading:
// 	   pass char to user task on its queue
int OpenR(_queue_id stream_no) {

	return 0;
}

int _getline(char* string) {
	return 0;
}

_queue_id OpenW(int task_id) {
	if (_mutex_try_lock(&write_mutex) == MQX_EOK) {
		// do things with write lock
		return HANDLER_QUEUE;
	}
	else {
		return 0; // error
	}
}

int _putline(_queue_id qid, char* string) {
	return 0;
}

int Close() {
	return 0;
}
